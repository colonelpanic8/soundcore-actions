Show mapped action names throughout Soundcore's earbud Controls page.

- The function picker popup and every assigned-gesture row now display the configured action name (for example **Paseo Live Voice** instead of **Anka Voice Assistant**, **Google Live Translate** instead of **AI Translation**).
- Original labels return as soon as a mapping is removed or custom mappings are disabled.
- Adds `docs/gesture-architecture.md`, an evidence-backed account of which earbud gestures can reach the phone. Existing mappings and app data are preserved.
